# BUILDING

This project is a SQL injection attack on my own local music streaming database app.

## Requirements

- Python 3
- Node.js
- My music streaming app running locally
- The Python `requests` library
- an oracle cloud AI autonomous database 
- an updated .env file with the data for your personal database
- take databases with kelly in order to have a nice 
database ready to be ravaged

Install the Python dependency with:

```bash
pip3 install requests